# english-quiz

## Запуск

Если отсутствует `gradle/wrapper/gradle-wrapper.jar` (он исключён из репозитория как бинарный файл),
сгенерируйте wrapper локально:

```bash
gradle wrapper --gradle-version 9.5.1
```

Далее запускайте проект:

```bash
./gradlew bootRun
```
